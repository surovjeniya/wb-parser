version: "3.7"

services:
  parser-api-service:
    build:
      context: ../app
      dockerfile: Dockerfile
      target: development
    command: npm run start:dev
    volumes:
      - ../app:/usr/src/app
    environment:
      HOST_NAME: http://localhost
